import { ModelComponent} from '../model.component';


/**
 * Component for importing a model via its definition as a TypeScript string 
 * Provides an UI to input the Typescript definition
 * Then gives it to a presenter for the actual importation to be performed
 */
export interface TSModelComponent extends ModelComponent {


}


