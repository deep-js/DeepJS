import { VisualizationInferenceComponent } from '../visualization-inference.component';

/**
 * Component allowing the user to give an input as an images and 
 * have the corresponding output from the current model
 * displayed 
 * Basically uses the model to make a prediction from images
 */
export interface VisualizationInferenceImageComponent extends VisualizationInferenceComponent {

}
