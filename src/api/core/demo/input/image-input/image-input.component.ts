import {InputComponent} from '../input.component'

/**
 * Component for importing image datasets
 * Includes a graphical interface allowing the user to give a list of files
 * that will be used as input for the model
 * Passes this list to its presenter who converts it to tensors
 */
export interface ImageInputComponent extends InputComponent{

	
}
