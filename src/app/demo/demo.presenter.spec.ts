import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DemoPresenter } from './demo.presenter';

describe('DemoPresenter', () => 
  let p: DemoPresenter;
  let fixture: ComponentFixture<DemoComponent>;

  beforeEach(() => {
  	p = new DemoPresenter
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });


});
